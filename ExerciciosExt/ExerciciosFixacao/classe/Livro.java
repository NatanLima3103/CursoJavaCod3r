package classe;

public class Livro {

    //Crie uma classe Livro com atributos: titulo (String), autor (String),
    // numeroPaginas (int), disponivel (boolean). Depois, numa classe
    // separada com main (pode chamar TesteLivro), crie dois objetos
    // diferentes da classe Livro, preencha os atributos de cada um com
    // valores diferentes, e imprima as informações dos dois.

    String titulo;
    String autor;
    int numeroPaginas;
    boolean disponivel;
}
